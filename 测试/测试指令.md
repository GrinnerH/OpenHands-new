# 执行agent
PYTHONPATH=/mnt/d/Work_space/Memory_agent/OpenHands-new:$PYTHONPATH \
DEBUG=1 ./run_secb.sh -m poc -l llm.deepseek -i 150 -n 1

# 执行LLM
uvicorn llmxcpgq_server:app --host 0.0.0.0 --port 8000

# 环境变量写进conda配置文件
vim $CONDA_PREFIX/etc/conda/activate.d/env_vars.sh

# 提取轨迹
python 测试/extract_all_trajectories.py

# 执行实例
export PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
export PIP_TRUSTED_HOST=pypi.tuna.tsinghua.edu.cn
conda activate secb
export DEBUG=True && ./run_secb.sh -m poc -l llm.DMXAPI -i 75 -n 3


python scripts/summarize_trace.py <trace.json> --format csv


## 构建项目docker

## 创建容器
sudo docker run -it \
  --name cpgvuln-openhands \
  -e OPENHANDS_PARENT_CONTAINER=cpgvuln-openhands \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -v /home/lab/Data_16T_2/wwh:/app/workspace \
  cpgvuln-openhands:latest /bin/bash

## 补充容器内部环境
apt update && apt install -y git
git --version
apt install -y vim
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
pip config list
echo 'export HF_ENDPOINT=https://hf-mirror.com' >> ~/.bashrc
source ~/.bashrc
pip install datasets

git config --global --add safe.directory /app

git remote set-url origin https://hub.gitmirror.com/https://github.com/GrinnerH/OpenHands-new.git

git fetch origin
git pull origin test
